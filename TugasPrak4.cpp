#include <bits/stdc++.h>
using namespace std;

int main(){
system("cls");
int banyak = 0;
int jumlah, data, total, nama, nilai;
cout << "Masukkan Jumlah Data Mahasiswa :"; cin >> jumlah;
data = data + banyak;
for(total; total < data; total++){
    cout << "Nama Lengkap : ";
    cin.ignore(cin >> ws, nama[total]);
    cout << "Nilai : ";
    cin >> nilai[total];
    cout << endl;
}

ratarata();
cout << endl << "Modus\t: " << modus << endl;

for (int i = 0; i < total; i++){
cout << "data Mahasiswa Ke-" << i+1 << endl;
cout << "Nama Lengkap : " << nama[i] << endl;
cout << "Nilai        : " << nilai[i] << endl;
}
system("pause");
 else {
    cout << "Belom Ada Data Yang Tersimpan" << endl;
    system("pause");
}
}

if(total > 0){
    system("cls");
    int modus = hitungModus(nilai, total):
    cout << "Rata-Rata \t: ";
    ratarata();
    cout << endl << "Modus\t: " << modus << endl;

for (int i = 0; i < total; i++) {
    int count = 0;
    for (int j = 0; j < total; j++) {
        if (nilai[j] == nilai[i]) {
            count++;
        }
void rata(){
    float rata = 0;
    for(int i = 0; i < total; i++){
        rata = rata + nilai[i];
    }
    rata = rata/total;
    cout << rata;
}

        }

        if (count > maxCount) {
            maxCount = count;
            modus = nilai[i];
        }
    }

    return modus;
}

int main() {
int pilihan, data = 0;

bool ulang = false;
}
