#include <iostream>
using namespace std;

int main () {
    // int x;
    // cin >> x;
// x = -1
    // if (x > 0) {
    //     cout << "positip\n";
    //     cout << "halo bang\n";
    // }

    // (kondisi) ? bener : salah;
    // (x > 0) 
    // ? cout << "positip\n"
    // : cout << "negatif\n";

    // int y = (x > 0) ? 10 : -99;

    // cout << y;

    int nilai;
    cout << "Masukkan nilai : "; cin >> nilai;

    // (kondisi1) ? benar1 : (kondisi2) ? benar2 : salah;
    (nilai >= 85);
}